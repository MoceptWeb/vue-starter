
const state = {
  list: []
}
const getters = {}

// actions
const actions = {
  bookMarkRequest: (state, data) => {
  }
}

// mutations
const mutations = {}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}
